<%= form_for(@views_email) do |f| %>
  <% if @views_email.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@views_email.errors.count, "error") %> prohibited this views_email from being saved:</h2>

      <ul>
      <% @views_email.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
